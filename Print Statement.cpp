class Solution {
  public:
    void utility() {
        string s;
        getline(cin,s);
        cout<<s<<endl;
    }
};

//Here remember the uses of getline 
// getline use to neglect the space between words
// example :-uses for Writing full name with spaces
